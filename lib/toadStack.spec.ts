import { describe, expect, it } from 'vitest'
import { ToadStack } from './toadStack'

describe('ToadStack', () => {
  it('Placeholder test', () => {
    // Please don't write a test like this
    const stack = new ToadStack()
    expect(stack).toBeDefined()
  })
})
